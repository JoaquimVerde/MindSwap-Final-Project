import { Suspense } from 'react';
import SearchBar from "@/app/ui/components/ui/search-bar";
import TeacherCourses from '@/app/ui/courses/teacher-courses';

export default function MyCoursesTeacher() {
    return (
        <TeacherCourses />
    );
}
