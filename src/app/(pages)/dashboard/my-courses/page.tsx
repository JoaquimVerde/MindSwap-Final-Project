// define the way that the courses appear


// page to display the list of courses that:
     // - if student:
          // they applied
               // application status
          // they're enrolled
               // check grades, upload projects

     // - if teacher:
          // the courses that they're teaching
               // check students that are enrolled in that course
               // give grades to students
               // check projects, check and give grades

     // if staff:
          // delete courses
          // edit courses information

export default function MyCourses() {
     return <p>My courses page</p>
     
}